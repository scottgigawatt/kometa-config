#
# Copyright 2025-2026 Scott Gigawatt
#
# Licensed under the Apache License, Version 2.0.
#
# ratings.yml: Keep experimental rating overlays separate from live wiring.
#

#
# Keep this local-asset experiment opt-in; the test-library runner uses the main ribbon set.
#
templates:

  #
  # Pair the IMDb logo with the audience score using equal queue weights.
  #
  imdb_rating:
    optional:
      - overlay_level
    default:
      overlay: <<overlay_name>>
    collection_level: <<overlay_level>>
    overlay:
      name: <<overlay_name>>
      file: /config/overlays/images/<<overlay>>.png
      queue: rating_queue_logo
      weight: <<weight>>

  #
  # Display the Plex audience score beside its matching IMDb logo.
  #
  audience_rating:
    optional:
      - overlay_level
    default:
      rating_type: audience_rating
    collection_level: <<overlay_level>>
    overlay:
      name: text(<<rating_type>>)
      queue: rating_queue_text
      weight: <<weight>>
      font: fonts/Inter-Medium.ttf
      font_size: 63
      font_color: "#FFFFFF"

  #
  # Select the Fresh logo independently from the numeric critic score.
  #
  rt_crit_fresh:
    optional:
      - overlay_level
    default:
      overlay: <<overlay_name>>
    collection_level: <<overlay_level>>
    overlay:
      name: <<overlay_name>>
      file: /config/overlays/images/<<overlay>>.png
      queue: rating_queue_logo
      weight: <<weight>>

  #
  # Use the Rotten logo for the lower critic-rating range.
  #
  rt_crit_rotten:
    optional:
      - overlay_level
    default:
      overlay: <<overlay_name>>
    collection_level: <<overlay_level>>
    overlay:
      name: <<overlay_name>>
      file: /config/overlays/images/<<overlay>>.png
      queue: rating_queue_logo
      weight: <<weight>>

  #
  # Keep the critic text formatting and weight paired with the Rotten Tomatoes logo.
  #
  critic_rating:
    optional:
      - overlay_level
    default:
      rating_type: critic_rating
    collection_level: <<overlay_level>>
    overlay:
      name: text(<<rating_type>>0)
      queue: rating_queue_text
      weight: <<weight>>
      font: fonts/Inter-Medium.ttf
      font_size: 63
      font_color: "#FFFFFF"

#
# Keep two bottom-row positions for logos and their matching score text.
# A named default layout is understood by both Kometa and its editor schema.
#
queues:

  #
  # Higher weights fill the left position first; a second match uses the right position.
  #
  rating_queue_logo:
    default:
      - horizontal_offset: 10
        horizontal_align: left
        vertical_offset: 10
        vertical_align: bottom
      - horizontal_offset: 320
        horizontal_align: left
        vertical_offset: 10
        vertical_align: bottom

  #
  # Offset text from its matching logo while preserving the same weight order.
  #
  rating_queue_text:
    default:
      - horizontal_offset: 189
        horizontal_align: left
        vertical_offset: 41
        vertical_align: bottom
      - horizontal_offset: 499
        horizontal_align: left
        vertical_offset: 41
        vertical_align: bottom

#
# Exclude missing or zero ratings and keep logo and text eligibility synchronized.
# IMDb uses weight 80; either Rotten Tomatoes logo and its text use weight 90.
#
overlays:
  imdb_rating:
    template:
      - name: imdb_rating
        weight: 80
    plex_search:
      all:
        audience_rating.gte: 0.1
        audience_rating.lte: 10

  audience_rating:
    template:
      name: audience_rating
      weight: 80
    plex_search:
      all:
        audience_rating.gte: 0.1
        audience_rating.lte: 10

  rt_crit_rotten:
    template:
      - name: rt_crit_rotten
        weight: 90
    plex_search:
      all:
        critic_rating.gte: 0.1
        critic_rating.lte: 6.0

  rt_crit_fresh:
    template:
      - name: rt_crit_fresh
        weight: 90
    plex_search:
      all:
        critic_rating.gte: 6.1
        critic_rating.lte: 10

  critic_rating:
    template:
      name: critic_rating
      weight: 90
    plex_search:
      all:
        critic_rating.gte: 0.1
        critic_rating.lte: 10
